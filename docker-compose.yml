services:
  salesmanagementsystem.api:
    image: salesmanagementsystem
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "8080:8080"

    environment:
      ASPNETCORE_ENVIRONMENT: Development  

    env_file:
      - .env

    restart: unless-stopped